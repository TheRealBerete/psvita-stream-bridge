# Copy to .env and adjust if needed. All variables are optional (defaults
# shown below are used when unset).

# How often (in hours) the background job re-checks every channel's health.
# ~10,000 channels at moderate concurrency take on the order of 30-45 minutes
# to fully check, so don't set this too low.
HEALTHCHECK_INTERVAL_HOURS=6
